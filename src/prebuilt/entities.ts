import {PrebuiltList} from './types.js'

export const entitiesList: PrebuiltList = {
  menuitem: {
    name: 'menuItem',
    desc: 'A simple menu item that displays a label, changes color on hover, and triggers an action when clicked.',
  },
}
